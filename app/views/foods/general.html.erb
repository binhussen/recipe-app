<section class="shoppinglist">
    <p>Shopping List</p>
    <div class="shopfood">
      <div>Amount of food items to buy: &nbsp;<%= @foods.length %></div>
      <div>Total value of food needed: &nbsp;$<%= @total %></div>
    </div>
    <table class="shoplistab">
      <thead>
        <tr>
          <th>Food</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% @foods.each do |food| %>
          <tr>
            <td><%= food.name %></td> 
            <td><%= food.quantity %></td>
            <td>$<%= food.price * food.quantity %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
</section>